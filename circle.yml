machine:
  ruby:
    version: 2.1.5
dependencies:
  post:
    - gem install capistrano
deployment:
  production:
    branch: master
    commands:
      - ssh -T git@github.com
      - cap production deploy
