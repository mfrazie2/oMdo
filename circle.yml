dependencies:
  post:
    - gem install rubygems-update
    - update_rubygems
    - gem update --system
    - gem install capistrano
deployment:
  production:
    branch: master
    commands:
      - cap production deploy
